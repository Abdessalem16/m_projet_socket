package client;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.net.Socket;

public class client {

    public static void main(String[] args) {
        try {
            // Se connecter au serveur sur le port 12345
            Socket socket = new Socket("localhost", 12345);
            System.out.println("Client 1 connecté au serveur.");

            // Créer des flux de lecture/écriture pour la communication avec le serveur
            BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));
            PrintWriter writer = new PrintWriter(socket.getOutputStream(), true);

            while (true) {
                // Lire un message depuis le clavier et l'envoyer au serveur
                System.out.print("Client 1, entrez un message (ou 'exit' pour quitter) : ");
                String message = reader.readLine();
                writer.println(message);

                if (message.equals("exit")) {
                    break;
                }
            }

            // Fermer la connexion
            socket.close();
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}
